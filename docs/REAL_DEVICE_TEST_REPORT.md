# A80 真实设备测试报告

测试日期：2026-07-16

测试对象：A80 手表（Android 8.1 / API 27，320 x 380）、MQTT 中继、HTTP 桥接服务和 Web 大屏

应用版本：`com.heatstress.watch` / `1.1.0-a80`（versionCode 2）

## 结论

本次在真实 A80 上完成了安装、开机自启动、前台采集服务、MQTT 在线状态、断线缓存补传、中文预警下发和
320 x 380 界面回归。桥接服务的单元测试、远端部署和 Web 端空值回归也已通过。手表未佩戴时，应用不会再把
零值或历史测试值伪装成实时生命体征。

当前结果证明 USB 联调链路可工作，但还不能视为生产现场验收通过。手表尚未配置独立网络和可信时间源，也没有
完成佩戴采集、GPS 室外定位和长时间续航测试；现有大屏后端还存在空健康值写入失败及历史值残留问题。

## 测试环境

| 项目 | 实测值 |
| --- | --- |
| 手表序列号 | `82432237337554` |
| Android | 8.1 / API 27 |
| 屏幕 | 320 x 380 |
| 应用 UID | 1000（平台共享 UID） |
| APK SHA-256 | `C2D20F80F174040FABFEBB35B398FB94E1010675F32095F93E8BC40F92C74725` |
| 签名证书 SHA-256 | `c8a2e9bccf597c2fb6dc66bee293fc13f2fc47ec77bc6b2b0d52c11f51192ab8` |
| 设备标识 | `A80-PROD-001` |
| MQTT | QoS 1，保留在线状态，LWT 离线状态 |

APK 从手表 `/data/app/.../base.apk` 回读后的哈希与本地 release 构建一致。

## 真实设备测试结果

| 场景 | 结果 | 证据与观察 |
| --- | --- | --- |
| 平台签名安装 | 通过 | package、versionCode、versionName、UID 和签名均与构建配置一致 |
| A80 厂商传感器接入 | 通过（未佩戴状态） | type 21 传感器可注册；`values[7] != 2` 时判定未佩戴 |
| 缺失生命体征处理 | 通过 | 未佩戴时心率、血氧、血压和体温保持空值，界面不显示伪造数据 |
| 320 x 380 界面 | 通过 | 主界面与中文预警完整显示，无文本遮挡或越界 |
| MQTT 上线与保留状态 | 通过 | 连接后状态主题返回 `online`，QoS 1 发布成功 |
| MQTT 断线缓存 | 通过 | 移除 ADB reverse 后队列累积 8 条；恢复后 8 条全部补传，队列归零 |
| MQTT 遗嘱 | 通过 | 链路断开后保留状态变为 `offline` |
| 中文预警下发 | 通过 | 手表收到中文标题和建议，通知页内容完整且不注入生命体征测试值 |
| 首次启动电源豁免 | 通过 | 应用写入 A80 的 autolaunch、secondarylaunch、wakelock、network 等免优化配置 |
| 重启自启动 | 通过 | 第二次冷启动回归中 BootReceiver 获准启动，SensorService 保持前台运行 |
| 桥接告警重试 | 通过 | 发布失败可重试；HTTP ACK 失败只重试 ACK，不重复向手表发布告警 |
| 桥接部署 | 通过 | 远端 systemd 服务 active，MQTT 已连接，设备状态可识别 |
| Web 空值快照 | 通过 | 新帧会替换旧生命体征，不再因对象合并残留历史值 |
| Web 空体温告警 | 通过 | 告警列表显示 `--`，浏览器无运行时异常 |
| Web MQTT 计数 | 通过 | 在线设备并入设备总数，离线数不会出现负数 |
| Web 降级地图 | 通过 | 未配置地图密钥时显示明确降级状态，不发起无效请求 |

## 自动化回归

- Bridge：`python -m unittest -v test_bridge.py`，5 项通过。
- Frontend：`npm run lint` 与 `npm run build` 通过，生产构建无 chunk-size 告警。
- Watch：release 单元测试和签名 APK 构建通过。
- Browser：真实浏览器加载、MQTT 在线数据、中文告警和空体温告警回归通过；最终页面无控制台错误。
- CI：GitHub Actions 覆盖 Bridge、Frontend 和 Watch 三部分构建与测试。

## 尚未完成的生产验收

1. **佩戴采集**：测试期间手表未实际佩戴，因此真实心率、血氧、血压、体温估算的准确性和连续性尚未验证。
2. **独立网络**：当前 A80 无可用 Wi-Fi 或 SIM，MQTT 依赖 USB `adb reverse`。脱离电脑后无法连接中继。
3. **可信时间**：设备重启后时钟回到 2012 年，且当前网络无法完成 NTP。桥接会拒绝无效时间戳，必须先解决 RTC/NTP。
4. **GPS**：室内无定位结果，尚未完成室外首次定位、漂移、断网和重启回归。
5. **大屏后端空值语义**：后端接收显式 null 健康字段时返回 500；省略字段又会保留历史测试值。修复前，大屏上的旧生命体征不能视为本次实测值。
6. **MQTT 安全**：现网明文端口允许匿名访问，TLS 证书也未建立可信链。生产部署需要关闭匿名访问、使用设备级凭据并启用可信 TLS。
7. **耐久性**：尚未执行 8 小时以上续航、后台保活、弱网漫游、积压上限和服务器故障恢复测试。

## 验收建议

在现场验收前，先为手表配置独立网络与可信时间同步，并修复大屏后端的可空字段更新语义。随后至少完成一次
真人佩戴的完整班次测试，记录原始传感器帧、手表展示值、MQTT 消息、后端落库值和大屏展示值，以同一时间轴
核对全链路一致性。测试告警必须使用独立编号并标注为测试数据，避免与真实热应激事件混淆。
